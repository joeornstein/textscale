% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/textscale.R
\name{textscale}
\alias{textscale}
\title{Measure a latent quantity from text}
\usage{
textscale(
  documents,
  prompt,
  prop = 0.8,
  n_train = 10000,
  n_test = 5000,
  seed = NULL,
  llm_model = "gpt-4.1-mini",
  embeddings_cache = "textscale_embeddings.rds",
  annotations_cache = "textscale_annotations.rds",
  annotations_path = NULL,
  method = "ridge",
  ci = TRUE,
  ci_method = "laplace",
  validate = TRUE,
  ...
)
}
\arguments{
\item{documents}{A character vector of documents to scale.}

\item{prompt}{Instruction text for the LLM annotator. Should be a
plain question or directive describing which document should be
judged "greater" on the latent dimension — no placeholder syntax
needed. For example: \code{"Which political ad is more negative toward its opponent?"}. The document text is appended automatically as
labelled options A and B.}

\item{prop}{Proportion of documents assigned to the training split.
Defaults to \code{0.8}.}

\item{n_train}{Maximum number of training comparison pairs to
generate. Defaults to \code{10000}.}

\item{n_test}{Maximum number of test comparison pairs to generate.
Defaults to \code{5000}.}

\item{seed}{Integer random seed for reproducible pair sampling.
Defaults to \code{NULL}.}

\item{llm_model}{OpenAI model name used for annotation. Defaults to
\code{"gpt-4.1-mini"}.}

\item{embeddings_cache}{File path for caching embeddings as an RDS
file. Passed to \code{\link[=get_embeddings]{get_embeddings()}}. Defaults to
\code{"textscale_embeddings.rds"} in the current working directory.
Set to \code{NULL} to disable caching.}

\item{annotations_cache}{File path for caching annotations as an RDS
file. Passed to \code{\link[=annotate_comparisons]{annotate_comparisons()}}. Defaults to
\code{"textscale_annotations.rds"} in the current working directory.
Set to \code{NULL} to disable caching.}

\item{annotations_path}{File path for checkpointing batch API calls.
Passed to \code{annotate_comparisons()} as \code{path}. Recommended for
runs with more than ~5,000 pairs. Defaults to \code{NULL}.}

\item{method}{Fitting method passed to \code{\link[=fit_model]{fit_model()}}. One of
\code{"ridge"} (default), \code{"lasso"}, \code{"enet"}, or \code{"svm"}.}

\item{ci}{Logical. If \code{TRUE}, the \code{scores} element of the returned
object is a tibble with \code{score}, \code{lower}, and \code{upper} columns.
Defaults to \code{TRUE}.}

\item{ci_method}{Method for computing confidence intervals. One of
\code{"laplace"} (default) or \code{"bootstrap"}. Passed to
\code{\link[=score_documents]{score_documents()}}. Ignored when \code{ci = FALSE}.}

\item{validate}{Logical. If \code{TRUE} (the default), \code{\link[=validate_model]{validate_model()}}
is called on the held-out test split and its output is printed.}

\item{...}{Additional arguments passed to \code{\link[=fit_model]{fit_model()}} (e.g.
\code{alpha}, \code{nlambda}, \code{lambda_min_ratio}).}
}
\value{
A \code{textscale_result} object (a list) containing:
\describe{
\item{\code{scores}}{Document scores on the latent dimension. A named
numeric vector by default, or a tibble with \code{score}, \code{lower},
and \code{upper} if \code{ci = TRUE}.}
\item{\code{model_final}}{The \code{textscale_model} fit on all comparisons,
used to produce \code{scores}. Pass to \code{\link[=score_documents]{score_documents()}} to scale
new documents.}
\item{\code{model_eval}}{The \code{textscale_model} fit on training
comparisons only, used for validation.}
\item{\code{metrics}}{Validation metrics from \code{\link[=validate_model]{validate_model()}}, or
\code{NULL} if \code{validate = FALSE}.}
\item{\code{comparisons}}{The annotated comparisons tibble.}
\item{\code{embeddings}}{The document embedding matrix.}
}
}
\description{
Runs the full textscale pipeline in a single call: generates pairwise
comparisons, retrieves embeddings, annotates pairs with an LLM, fits
and validates a model on the train/test split, refits on all
comparisons, and returns scores for every document.
}
