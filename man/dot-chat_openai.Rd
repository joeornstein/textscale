% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_comparisons.R
\name{.chat_openai}
\alias{.chat_openai}
\title{Annotate pairwise comparisons using an LLM}
\usage{
.chat_openai(...)
}
\arguments{
\item{...}{Additional arguments passed to
\code{\link[ellmer:batch_chat]{ellmer::batch_chat_text()}} or \code{\link[ellmer:parallel_chat]{ellmer::parallel_chat_text()}}.}

\item{comparisons}{A tibble produced by
\code{\link[=generate_comparisons]{generate_comparisons()}}.}

\item{prompt}{An \code{\link[ellmer:interpolate]{ellmer::interpolate()}} template string. Use
\code{{{text_a}}} and \code{{{text_b}}} to reference the two documents being
compared. Example: \code{"Which is heavier?\\\\nA: {{text_a}}\\\\nB: {{text_b}}"}.}

\item{model}{Character string naming the OpenAI model to use.
Defaults to \code{"gpt-4.1-mini"}.}

\item{system_prompt}{System prompt sent to the LLM. Defaults to
instructing the model to respond with a single letter.}

\item{path}{Optional file path for checkpointing batch API calls
(passed to \code{\link[ellmer:batch_chat]{ellmer::batch_chat_text()}}). Useful for resuming
interrupted runs. If \code{NULL}, uses \code{\link[ellmer:parallel_chat]{ellmer::parallel_chat_text()}}
instead.}

\item{cache}{Optional path to an \code{.rds} file. If the file exists,
cached annotations are matched to the incoming \code{comparisons} by
\code{text_a} and \code{text_b}. Rows whose text pair is found in the cache
reuse the stored \code{winner}; only rows without a cache hit are sent
to the LLM. The updated result (cached + new) is written back to
\code{cache} after annotation. If \code{cache} is \code{NULL} all rows are
annotated and nothing is written to disk.}
}
\value{
The input \code{comparisons} tibble with an additional \code{winner}
column containing \code{"A"} or \code{"B"} for each pair.
}
\description{
Submits each comparison pair to an LLM and records which document
"wins" on the latent dimension of interest. The \code{prompt} is
interpolated for each row using \code{\link[ellmer:interpolate]{ellmer::interpolate()}}, so it may
reference any column in \code{comparisons} with \code{{{column_name}}} syntax.
Most prompts will use \code{{{text_a}}} and \code{{{text_b}}}.
}
\details{
When \code{path} is supplied the underlying \code{\link[ellmer:batch_chat]{ellmer::batch_chat_text()}}
call caches its results to that file, so interrupted jobs can be
resumed without re-calling the API.
}
