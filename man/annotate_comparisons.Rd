% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_comparisons.R
\name{annotate_comparisons}
\alias{annotate_comparisons}
\title{Annotate pairwise comparisons using an LLM}
\usage{
annotate_comparisons(
  comparisons,
  instructions = NULL,
  prompt = "A: {{text_a}}\\nB: {{text_b}}",
  model = "gpt-5.4-mini",
  document_type = c("text", "image"),
  resize = "high",
  system_prompt = NULL,
  allow_ties = TRUE,
  path = "textscale_annotations.json",
  parallel = FALSE,
  cache = NULL,
  ...
)
}
\arguments{
\item{comparisons}{A tibble produced by
\code{\link[=generate_comparisons]{generate_comparisons()}}.}

\item{instructions}{A plain-language description of the comparison
task (e.g., \code{"Which text is more conservative?"}). Prepended to
\code{system_prompt} so the LLM knows what to judge. When using
\code{annotate_comparisons()} on its own, this is typically the only
argument you need beyond \code{comparisons}.}

\item{prompt}{An \code{\link[ellmer:interpolate]{ellmer::interpolate()}} template string for
formatting each document pair. Defaults to
\code{"A: {{text_a}}\\\\nB: {{text_b}}"}. Override this only if you need
a non-standard document layout. Ignored when
\code{document_type = "image"}.}

\item{model}{Character string naming the OpenAI model to use.
Defaults to \code{"gpt-5.4-mini"}. When \code{document_type = "image"}, must
name a vision-capable model.}

\item{document_type}{One of \code{"text"} (default) or \code{"image"}. When
\code{"image"}, \code{text_a}/\code{text_b} in \code{comparisons} are treated as paths
to local image files: each is attached to the LLM turn via
\code{\link[ellmer:content_image_url]{ellmer::content_image_file()}} (labelled "Image A:"/"Image B:")
instead of being interpolated into \code{prompt}, and the default
\code{system_prompt} notes that the images are shown in that order.}

\item{resize}{Passed to \code{\link[ellmer:content_image_url]{ellmer::content_image_file()}} as the
\code{resize} argument. Only used when \code{document_type = "image"}.
Defaults to \code{"high"}.}

\item{system_prompt}{System prompt sent to the LLM. When \code{NULL}
(the default), an appropriate prompt is generated based on the
\code{allow_ties} argument. Supply a custom value to override this
behaviour entirely. When \code{instructions} is supplied, it is
prepended to \code{system_prompt}.}

\item{allow_ties}{Logical. If \code{TRUE} (the default), the LLM may
respond with \code{"tie"} when the two texts are indistinguishable on
the dimension of interest. Ties are recorded in the \code{winner}
column and automatically dropped by downstream functions
(\code{\link[=fit_model]{fit_model()}}, \code{\link[=validate_model]{validate_model()}}). If \code{FALSE}, the system
prompt instructs the LLM to choose A or B with no ties allowed.
Ignored when a custom \code{system_prompt} is supplied.}

\item{path}{File path for checkpointing batch API calls (passed to
\code{\link[ellmer:batch_chat]{ellmer::batch_chat_text()}}). Defaults to
\code{"textscale_annotations.json"} in the current working directory.
Deleted automatically once the batch completes and results are
written to \code{cache}. Set to \code{NULL} to disable checkpointing.
Ignored when \code{parallel = TRUE}.}

\item{parallel}{Logical. If \code{FALSE} (the default), annotations are
submitted via the OpenAI Batch API at 50\% of standard prices.
Set to \code{TRUE} to use \code{\link[ellmer:parallel_chat]{ellmer::parallel_chat_text()}} for immediate
results at standard prices.}

\item{cache}{Optional path to an \code{.rds} file. If the file exists,
cached annotations are matched to the incoming \code{comparisons} by
\code{text_a} and \code{text_b}. Rows whose text pair is found in the cache
reuse the stored \code{winner}; only rows without a cache hit are sent
to the LLM. The updated result (cached + new) is written back to
\code{cache} after annotation. If \code{cache} is \code{NULL} all rows are
annotated and nothing is written to disk. Caches written by
textscale carry a prompt hash and will be ignored if the prompt
changes; externally supplied caches (no hash attribute) bypass
this check.}

\item{...}{Additional arguments passed to
\code{\link[ellmer:batch_chat]{ellmer::batch_chat_text()}} or \code{\link[ellmer:parallel_chat]{ellmer::parallel_chat_text()}}.}
}
\value{
The input \code{comparisons} tibble with an additional \code{winner}
column containing \code{"A"}, \code{"B"}, or (when \code{allow_ties = TRUE})
\code{"tie"} for each pair.
}
\description{
Submits each comparison pair to an LLM and records which document
"wins" on the latent dimension of interest.
}
\details{
The simplest way to use this function is to supply \code{instructions}
describing the comparison task in plain language (e.g.,
\code{"Which text is more conservative?"}). The instructions are
automatically prepended to the system prompt, and each LLM turn
contains the two documents formatted as
\code{"A: <text_a>\\\\nB: <text_b>"}.

For advanced use, the \code{prompt} argument is an
\code{\link[ellmer:interpolate]{ellmer::interpolate()}} template string that may reference any
column in \code{comparisons} with \code{{{column_name}}} syntax (most
commonly \code{{{text_a}}} and \code{{{text_b}}}).

By default, annotations are submitted via the OpenAI Batch API
(\code{\link[ellmer:batch_chat]{ellmer::batch_chat_text()}}), which is 50\% cheaper than standard
pricing but may take up to 24 hours to complete. The \code{path} argument
checkpoints batch progress to a \code{.json} file so interrupted jobs can
be resumed without re-calling the API. Set \code{parallel = TRUE} to use
\code{\link[ellmer:parallel_chat]{ellmer::parallel_chat_text()}} instead, which returns results
immediately at standard API prices.

Before sending requests to the API, the function prints an estimated
cost based on approximate token counts (1 token ≈ 4 characters) and
published OpenAI prices. The estimate assumes one output token per
comparison (single-letter response). Prices may be out of date; see
\url{https://openai.com/api/pricing/} for current rates.
}
